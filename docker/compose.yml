services:
  sagwm-command-central:
    container_name: "sag-command-central"
    build:
      context: .
      dockerfile: centos.Dockerfile
      args:
        - CC_INSTALLER_PATH=installer/cc-def-10.15-fix8-lnxamd64.sh
        # - CC_ADMIN_HOST=sagbase.wilow.local
    ports: # 8090 8091 8092 8093 8094 8095
      - "8091:8091"
      - "8090:8090"
      # - "8092:8092"
      

  # sagwm-command-central-win:
  #   container_name: "sag-command-central"
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.windows
  #     args:
  #       - CC_INSTALLER_PATH=installer/cc-def-10.15-fix8-w64.bat
  #       - CC_ADMIN_HOST=sagbase.wilow.local
  #   ports:
  #     - "8091:8091"
      